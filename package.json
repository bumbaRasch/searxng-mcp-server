{
  "name": "searxng-mcp-server",
  "version": "0.3.2",
  "mcpName": "io.github.bumbaRasch/searxng-mcp-server",
  "description": "MCP server for a self-hosted SearXNG instance: web, image, news, video and music search plus page fetch.",
  "type": "module",
  "license": "MIT",
  "author": "Dimitri Pisarev",
  "repository": {
    "type": "git",
    "url": "https://github.com/bumbaRasch/searxng-mcp-server.git"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "mcp-server",
    "searxng",
    "search",
    "web-search",
    "fetch",
    "readability",
    "privacy"
  ],
  "packageManager": "pnpm@12.5.1",
  "engines": {
    "node": ">=22.19.0"
  },
  "bin": {
    "searxng-mcp-server": "dist/index.js"
  },
  "files": [
    "dist",
    "docs/design.md",
    "docs/extending.md",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "scripts": {
    "prebuild": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\"",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "prepublishOnly": "pnpm lint && pnpm lint:types && pnpm typecheck && pnpm test && pnpm build",
    "lint": "oxlint",
    "lint:types": "oxlint --type-aware",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "inspector": "npx -y @modelcontextprotocol/inspector node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/node": "^2.0.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "@mozilla/readability": "^0.6.0",
    "linkedom": "^0.18.13",
    "turndown": "^7.2.4",
    "undici": "^8.10.2",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "@types/node": "^26.6.2",
    "@types/turndown": "^5.0.6",
    "@vitest/coverage-v8": "^5.0.1",
    "oxlint": "^1.83.0",
    "oxlint-tsgolint": "^7.0.2002",
    "prettier": "^3.9.8",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
